import { RequestConfig } from '@/uni_modules/kux-request';
import { http } from '../http/index';

export const loginBySms = () : Promise<any> => {
  return http.post('/login/mobilelogin', { debug: true } as RequestConfig);
}